img {
    object-fit: cover;
    opacity: 0;
    transform: translateY(50%);
    transition: all 0.5s;
}

.fade {
    transform: translateY(0);
    transition: all 0.5s;
    opacity: 1;
}

.notFade {
    opacity: 1;
    transform: translateY(0);
    transition:none;
}