function showProgress() {
    parent.showProgress();
}

function hideProgress() {
    parent.hideProgress();
}
/*Menu Click Event*/
function openMenuTarget(target) {
    parent.openMenuTarget(target);
}
function openMenuPath(path, queryString = undefined, reload = false) {
    parent.openMenuPath(path, queryString, reload);
}
function openMenuKey(key, queryString = undefined, reload = false) {
    parent.openMenuKey(key, queryString, reload);
}
//Tab Open 함수
function openTab(target) {
    parent.openTab(target);
}
//Tab Close 함수
function closeTab(key) {
    parent.closeTab(key);
}
function closeTabPath(path) {
    parent.closeTabPath(path);
}

function Reload(path) {
    parent.Reload(path);
}